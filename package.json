{
  "name": "reward-spin",
  "version": "1.0.0",
  "description": "RewardSpin - Telegram Mini App for earning points",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "client": "cd client && npm start",
    "install-all": "npm install && cd client && npm install",
    "build": "cd client && npm run build --no-sourcemap"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.4",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "node-telegram-bot-api": "^0.64.0",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
